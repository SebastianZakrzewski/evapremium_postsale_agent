# Copy to `.env` for local development (`.env` is gitignored).
# See project agent orchestration for how these are read.

# --- Production (docker-compose.prod.yml): required. Image must exist in GHCR first (CI push on main).
# POSTSALE_AGENT_IMAGE=ghcr.io/sebastianzakrzewski/evapremium_postsale_agent:main

# --- Dev / build on server (docker-compose.yml): optional. Tags the built image when set.
# POSTSALE_AGENT_IMAGE=ghcr.io/sebastianzakrzewski/evapremium_postsale_agent:main

# HTTP port (optional; default 3002 in main.ts)
PORT=3002

# LangChain chat model id passed to createAgent (optional; code has a default)
POSTSALE_AGENT_CHAT_MODEL=openai:gpt-4o-mini

# OpenAI API key when using OpenAI-backed models
OPENAI_API_KEY=

# Optional LangSmith tracing (see https://docs.langchain.com/langsmith/env-var)
# LANGCHAIN_TRACING_V2=true
# LANGSMITH_API_KEY=
